# DDD Framework Environment Configuration
# Copy this file to .env and customize for your project

# Core paths for DDD testing and demo
DDD_PROJECT_ROOT=./baseline/ansible     # Project to analyze
DDD_BASELINE_PATH=./baseline/ansible     # Real modules to test against
DDD_OUTPUT_DIR=./docs/generated          # Where runbooks go

# Optional overrides (sensible defaults in code)
DDD_COVERAGE_THRESHOLD=85                # Override default threshold
DDD_VERBOSE=false                         # Set true for demo walkthrough
DDD_DEBUG=false                           # Enable debug logging

# Sphinx documentation settings (optional)
DDD_SPHINX_THEME=sphinx_rtd_theme        # Documentation theme
DDD_SPHINX_AUTOBUILD_PORT=8000           # Port for live docs preview